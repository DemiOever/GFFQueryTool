package nl.bioinf.alpruis.operations;

// this is where all the classes for the options that can be used in the cmd interface are defined and have been made
// this includes the following options:
// --help, -v, -vv, -vvv, --validate, --summary, --fetch_id, --fetch_type,
// --fetch_attribute, --fetch_features, --fetch_region, --find_sites(???)

public class OperationFetchingAFIT {
    public static class fetchAttributes {

    }

    public static class fetchFeatures {

    }

    public static class fetchID {

    }

    public static class fetchType {

    }
}
